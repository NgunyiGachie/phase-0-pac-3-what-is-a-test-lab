const name = "Susan";
let height = 10;
if (height > 0 && height <40) {console.log("height is greater than 0 but less than 40")};

const message = `${name} is ${height} inches tall`;

module.exports = {name, height, message};
